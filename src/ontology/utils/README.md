These scripts are legacy and ad hoc helpers from earlier BERVO curation rounds.

They are not part of the ODK build pipeline or CI release flow.

Use them only for manual curation tasks, and prefer keeping any new one-off scripts in this directory rather than the `src/ontology/` root.
